import { Navbar, Modal } from '../components';

export function Hero() {
    return (
        <section className='w-full h-full hero-img py-9'>
            <Modal />
            <Navbar />
        </section>
    );
}
